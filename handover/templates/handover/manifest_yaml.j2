# .handover/manifest.yaml
# Vendor-neutral metadata about this generated knowledge base.
schema_version: "{{ scaffold.manifest.schema_version }}"
version: "{{ scaffold.manifest.version }}"
generated_at: "{{ scaffold.manifest.generated_at }}"
source: "{{ scaffold.manifest.source }}"
target: "{{ scaffold.manifest.target }}"
project: "{{ scaffold.manifest.project }}"
